import React from 'react';

export default class Navigation extends React.Component {
  render() {
    return (
      <div id="navbar">
        <button className="btn btn-nav pull-right">logout</button>
      </div>
    );
  }
}
